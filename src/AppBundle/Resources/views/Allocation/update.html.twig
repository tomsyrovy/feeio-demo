{% extends "::layout-secured-simple.html.twig" %}

{#{% if allocationContainer.yearmonth %}#}
    {#{% set title = "Alokace pro " ~ commission.name ~ ": " ~ allocationContainer.yearmonth.code %}#}
{#{% else %}#}
    {% set title = "Rozpočet pro " ~ commission.name ~ ": verze " ~ allocationContainer.version %}
{#{% endif %}#}



{% block title %}{{ title }} {{ titleSeparator }} {{ parent() }}{% endblock %}
{% block body_class %}page-contact-create {{ parent() }}{% endblock %}


{% block content %}


    <div class="row">

        <div class="col-lg-12">
            {#{% if allocationContainer.yearmonth %}#}
                {#<a class="btn btn-info" title="Zpět" href="{{ path('app_allocation_list', {'commission_id': commission.id}) }}"><i class="fa fa-fw fa-caret-left"></i> Zpět</a>#}
            {#{% else %}#}
                <a class="btn btn-info" title="Zpět" href="{{ path('app_commission_detail', {'commission_id': commission.id}) }}"><i class="fa fa-fw fa-caret-left"></i> Zpět</a>
            {#{% endif %}#}

            <div class="page-header text-center">
                <h2><i class="fa fa-fw fa-briefcase"></i> {{ title }}</h2>
            </div>
        </div> <!-- .col-lg-12 -->

        {% include "@App/Allocation/inc/form.html.twig" with {"form" : form, "submitLabel" : "Uložit"} %}


    </div> <!-- .row -->


{% endblock %}

{% block javascripts %}

    {{ parent() }}

    {% include '@App/Allocation/inc/formJS.html.twig' with {"commission" : commission} %}

{% endblock %}
